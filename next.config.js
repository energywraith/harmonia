/** @type {import('next').NextConfig} */
const nextConfig = { images: { domains: ['assets.genius.com', 'images.genius.com', 's3.amazonaws.com', 'images.rapgenius.com'], formats: ['image/avif', 'image/webp'], }, }

module.exports = nextConfig